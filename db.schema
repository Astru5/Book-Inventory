CREATE TABLE "Inventory" (`ID` INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE NOT NULL, `Title` TEXT NOT NULL, `Author` TEXT NOT NULL, `Genre` TEXT NOT NULL, `Publication Date` TEXT NOT NULL, `ISBN` INTEGER UNIQUE NOT NULL)